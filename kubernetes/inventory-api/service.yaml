apiVersion: v1
kind: Service

metadata:
  name: inventory-api
  labels:
    app: inventory-api

spec:
  type: ClusterIP

  selector:
    app: inventory-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000

