apiVersion: v1
kind: Service

metadata:
  name: order-api
  labels:
    app: order-api

spec:
  type: ClusterIP

  selector:
    app: order-api

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
