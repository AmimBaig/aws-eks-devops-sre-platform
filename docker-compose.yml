services:

  order-api:
    build:
      context: ./applications/order-api
    container_name: order-api
    ports:
      - "8000:8000"
    environment:
      INVENTORY_API_URL: http://inventory-api:8000
    depends_on:
      - inventory-api

  inventory-api:
    build:
      context: ./applications/inventory-api
    container_name: inventory-api
    ports:
      - "8001:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
    depends_on:
      - redis

  redis:
    image: redis:7-alpine
    container_name: inventory-redis
    ports:
      - "6379:6379"
